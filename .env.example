# PolyEdge Automator v2.5 — Environment Variables
# Copy to .env and fill in your values.
# NEVER commit .env to version control.

# OpenRouter API key for AI swarm
OPENROUTER_API_KEY=sk-or-REPLACE_ME

# Polymarket API key
POLYMARKET_API_KEY=REPLACE_ME

# Telegram bot token
TELEGRAM_BOT_TOKEN=REPLACE_ME

# Local state secret (HMAC for state signature and arming file)
LOCAL_STATE_SECRET=REPLACE_ME

# Database URL (default for docker-compose)
POLYEDGE_DATABASE_URL=postgresql://polyedge:polyedge_dev@localhost:5432/polyedge
